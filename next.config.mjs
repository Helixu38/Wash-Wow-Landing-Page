/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["firebasestorage.googleapis.com"],
    domains: ["images.unsplash.com", "www.w3.org"],
  },
};

export default nextConfig;
